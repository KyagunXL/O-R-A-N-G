const Discord = require("discord.js");

module.exports.run = async (bot, message, args) => {

    message.channel.send("The Council of O R A N G and the Borats has't did bless thee")

}

module.exports.help = {
    name: "praise"
}